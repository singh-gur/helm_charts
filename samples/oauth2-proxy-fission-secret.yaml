apiVersion: v1
kind: Secret
metadata:
  name: oauth2-proxy-fission-secret
  namespace: fission
type: Opaque
stringData:
  client-id: "YOUR_ZITADEL_CLIENT_ID"
  client-secret: "YOUR_ZITADEL_CLIENT_SECRET"
  cookie-secret: "GENERATE_WITH: openssl rand -base64 32 | head -c 32"
