{{- if .Values.uptime.enabled }}
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  namespace: {{ .Values.argocdNamespace | default "default" }}
  name: uptime
  finalizers:
  - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: https://dirsigler.github.io/uptime-kuma-helm
    chart: uptime-kuma
    targetRevision: {{.Values.uptime.version}}
    helm:
      values: |
        image:
          repository: {{ .Values.uptime.image.repository }}
          {{- if .Values.uptime.image.pullPolicy }}
          pullPolicy: {{ .Values.uptime.image.pullPolicy }}
          {{- end }}
          {{- if .Values.uptime.image.pullSecrets }}
          pullSecrets:
            {{- range .Values.uptime.image.pullSecrets }}
            - name: {{ . }}
            {{- end }}
          {{- end }}
        ingress:
          enabled: true
          hosts:
            - host: {{.Values.uptime.ingress.host}}
              paths:
                - path: /
                  pathType: Prefix
  destination:
    server: https://kubernetes.default.svc
    namespace: default
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
{{- end }}
