{{- if .Values.enabled }}
apiVersion: fission.io/v1
kind: HTTPTrigger
metadata:
  name: {{ .Values.function.name }}-trigger
  namespace: {{ .Values.namespace }}
spec:
  functionref:
    name: {{ .Values.function.name }}
    type: name
  method: {{ .Values.httptrigger.method }}
  relativeurl: {{ .Values.httptrigger.url }}
{{- end }}
