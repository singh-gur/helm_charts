apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: whoami

spec:
  rules:
    - http:
        paths:
          - path: /knock
            pathType: Prefix
            backend:
              service:
                name: whoami
                port:
                  number: 5678
